{{#if App.currentUser }}
  {{render 'card'}}
  {{render 'skills'}}
  {{render 'experiences'}}
  {{render 'educations'}}
  {{render 'languages'}}
  {{render 'hobbies'}}
  {{ outlet }}
{{else}}
  {{partial "landing"}}
{{/if}}
