{{#if controller.displayable }}
  {{#view App.FocusView }}
  <div class="col-md-12">
    <div class='col-md-4'>Hobbies</div>
    <div class='col-md-8'>
      <ul class="nav nav-list">
        {{#each controller}}
        <li>{{ name }}</li>
        {{/each}}
      </ul>
    </div>
  </div>
  {{/view}}
{{/if}}
