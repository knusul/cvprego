{{#if controller.displayable }}
  {{#view App.FocusView }}
  <div class="span12">
    <div class='span4'>Hobbies</div>
    <div class='span8'>
      <ul class="nav nav-list">
        {{#each controller}}
        <li>{{ name }}</li>
        {{/each}}
      </ul>
    </div>
  </div>
  {{/view}}
{{/if}}
