!!!
%html
  %head
    %title How to write a CV? CVPrego the Resume generation application
    - if user_signed_in?
      %meta{:content => UserSerializer.new(current_user).to_json(root: false), :name => "current-user"}/
      %meta{content: "pdf, generation, cv, curriculum, curriculum vitae, resume, cover letter, letter, c.v", name: "keywords"}/
      %meta{content: "Quick and robust CV generation tool", name: "description"}/
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = javascript_include_tag params[:controller]
    = csrf_meta_tags
    =render 'google_analytics'
    =favicon_link_tag "favicon.png"
  %body
    .container
      = render 'navigation'
      .content
        = yield
      =render 'footer'
